# Finding image in a directory based on text content

Note: Add test_images/ or a directory for test images to root before running app/clovaai-craft/clovaai-craft-master/test.py and change path in file.